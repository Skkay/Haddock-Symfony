{% extends "admin/base.html.twig" %}

{% block title "Administration - Juron" %}

{% block page_title "Administration - Juron" %}

{% block buttons_area %}
<a href="{{ path('admin.juron.create') }}" class="btn btn-outline-primary" role="button">Ajouter un juron</a>
{% endblock %}

{% block body %}
<div class="table-responsive">
    <table class="table table-hover table-sm table-striped" id="table_albums">
        <thead>
            <tr>
                <th class="text-center">Numéro</th>
                <th>Texte</th>
                <th class="text-center" colspan="2">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for juron in jurons %}
            <tr>
                <td class="text-center">{{juron.num}}</td>
                <td>{{juron.texte}}</td>
                <td class="text-center"><a class="text-success" href="{{ path('admin.juron.edit', {num: juron.num}) }}">Modifier</a></td>
                <td class="text-center"><a class="text-danger" href="{{ path('admin.juron.delete', {num: juron.num}) }}">Supprimer</a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}

{% block javascripts %}

{% endblock %}

