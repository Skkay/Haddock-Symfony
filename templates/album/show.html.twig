{% extends "base.html.twig" %}

{% block title album.titre %}

{% block body %}
<div class="container mt-4">
    <div class="jumbotron">
        <div class="media">
            <img src="{{ asset('images/albums/') }}{{album.image}}.jpg" width="157px" height="212px" class="align-self-start mr-3">
            <div class="media-body">
                <h5 class="mt-0">{{ album.titre }}</h5>
                C'est l'album n°{{ album.tome }} paru en {{ album.parution }}. 
                {% if nb_jurons < 1 %}Il ne contient aucun juron.{% endif %}
                {% if nb_jurons == 1 %}Il contient seulement un juron :{% endif %}
                {% if nb_jurons > 1 %}Il contient ces {{ nb_jurons }} jurons :{% endif %}                
                <ul>
                    {% for juron in list_jurons %}
                        <li><b>{{ juron['jurons_texte'] }}</b> à la page {{ juron['num_page'] }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</div>
{% endblock %}
